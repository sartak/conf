[user]
	name = Shawn M Moore
	email = sartak@gmail.com
[color]
	diff = auto
	status = auto
	branch = auto
	ui = auto
[alias]
	up = pull --rebase
	ci = !git stash >/dev/null && git svn dcommit && git stash apply >/dev/null
    u = !git stash >/dev/null && git svn rebase && git stash apply >/dev/null
	st = status
	record = !git add -p && git commit
	co = checkout
	br = branch
[github]
	user = sartak

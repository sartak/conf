" search as you type
set incsearch

" highlight search matches
set hlsearch

" lowercase search strings are case insensitive
set ignorecase
set smartcase

" hit \/ turn off highlighting after a search
nmap <silent> <leader>/ :nohl<CR>

